<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:local="clr-namespace:BasicEnglishTest" 
		x:Class="BasicEnglishTest.QuestionView">
	<ContentPage.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			
			<!-- Question section -->
			<StackLayout HorizontalOptions="Start" VerticalOptions="Start" Grid.Row="0">
				<Label Text="{Binding Index, StringFormat='Question {0}:'}" />
				<ScrollView>
					<Label Text="{Binding Content}" />
				</ScrollView>
			</StackLayout>
			
			<!-- Answer section -->
			<StackLayout x:Name="lvAnswers" HorizontalOptions="Start" VerticalOptions="End" BackgroundColor="Red">
				
			</StackLayout>
			
		</Grid>
	</ContentPage.Content>
</ContentPage>
