<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:local="clr-namespace:BasicEnglishTest" 
		x:Class="BasicEnglishTest.QuestionView"
		Padding="20"
		>
	<ContentPage.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			
			<!-- Question section -->
			<StackLayout HorizontalOptions="Start" VerticalOptions="Start" Grid.Row="0">
				<Label x:Name="numQuest" Text="{Binding Index, StringFormat='Question {0}:'}" FontAttributes="Bold" />
				<BoxView HeightRequest="0.5" BindingContext="{x:Reference numQuest}" WidthRequest="{Binding Width}"  BackgroundColor="Black" />
				<ScrollView>
					<Label Text="{Binding Content}" />
				</ScrollView>
			</StackLayout>
			
			<!-- Answer section -->
			<StackLayout x:Name="lvAnswers" HorizontalOptions="Start" VerticalOptions="End" >
				
			</StackLayout>
			
		</Grid>
	</ContentPage.Content>
</ContentPage>
